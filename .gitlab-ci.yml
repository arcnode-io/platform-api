stages:
  - check
  - publish
  - smoke
  - mirror

checks:
  stage: check
  variables:
    UV_FROZEN: "true"
  script:
    - uv run poe checks
    - uv run pytest --junitxml=junit.xml
    - uv run poe cover
    - uv run coverage xml
  coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'
  artifacts:
    when: always
    reports:
      junit: junit.xml
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
build:
  stage: publish
  script:
    - aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws
    - docker build -t public.ecr.aws/y1d2j6a8/platform-api .
    - docker push public.ecr.aws/y1d2j6a8/platform-api

publish_compose:
  stage: publish
  script:
    # Per-variant docker-compose, and init
    # scripts are static artifacts EC2 UserData fetches at boot. No
    # versioning prefix — latest in main always wins. Customer EC2 caches
    # locally on first pull, so already-deployed stacks are unaffected.
    # Excludes keep platform-api Python toolchain leftovers (pycache,
    # *_test.py) out of the public bucket — those don't ship to customers.
    - aws s3 sync src/compose/commercial/    s3://arcnode-public/compose/commercial/    --delete --exclude "__pycache__/*" --exclude "*.pyc"
    - aws s3 sync src/compose/defense/       s3://arcnode-public/compose/defense/       --delete --exclude "__pycache__/*" --exclude "*.pyc"
    - aws s3 sync src/compose/init-scripts/  s3://arcnode-public/init-scripts/          --delete --exclude "__pycache__/*" --exclude "*.pyc" --exclude "*_test.py"
    - aws s3 cp src/compose/industrial-fixtures.json s3://arcnode-public/seed/industrial-fixtures.json
    - aws s3 rm s3://arcnode-public/seed/dtm-smoke.json || true
  only:
    - main

smoke_commercial:
  # Real-AWS e2e — deploys the commercial stack, asserts gateway publishes
  # via industrial-fixtures mocks into Tiger Cloud, tears down. Catches
  # CFN / UserData / VPC / IAM bugs unit + integration tests can't.
  # Cost: ~$0.50/run, ~10min cycle.
  stage: smoke
  variables:
    SMOKE_STACK: "smoke-ci-$CI_PIPELINE_ID"
    # Slugified for both compose-DNS topic prefixes (sites/<id>/...) and
    # row scoping in Tiger so concurrent CI runs don't collide.
    SITE_ID: "ci_$CI_PIPELINE_ID"
  script:
    - bash tools/launch-arcnode-smoke-commercial.sh "$SMOKE_STACK"
    - bash tools/smoke-wait-stack-ready.sh "$SMOKE_STACK"
    - bash tools/smoke-validate-commercial.sh
  after_script:
    # Idempotent — kicks off DELETE whether or not earlier steps passed.
    - aws cloudformation delete-stack --stack-name "$SMOKE_STACK" --region us-east-1 || true
  only:
    - main
  timeout: 30 minutes

mirror:
  stage: mirror
  variables:
    GIT_STRATEGY: clone
    GIT_DEPTH: 0
  script:
    - git checkout -B main
    - git push --mirror git@github.com:arcnode-io/platform-api.git
  only:
    - main
